_schema-version: "3.2"
ID: serviceapproval
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: serviceapproval-destination-content
  type: com.sap.application.content
  requires:
  - name: serviceapproval-destination-service
    parameters:
      content-target: true
  - name: serviceapproval-repo-host
    parameters:
      service-key:
        name: serviceapproval-repo-host-key
  - name: serviceapproval-uaa
    parameters:
      service-key:
        name: serviceapproval-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: serviceapproval_html_repo_host
          ServiceInstanceName: serviceapproval-html5-service
          ServiceKeyName: serviceapproval-repo-host-key
          sap.cloud.service: serviceapproval
        - Authentication: OAuth2UserTokenExchange
          Name: serviceapproval_uaa
          ServiceInstanceName: serviceapproval-xsuaa-service
          ServiceKeyName: serviceapproval-uaa-key
          sap.cloud.service: serviceapproval
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: serviceapproval-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: serviceapproval-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - serviceapproval.zip
      name: serviceapproval
      target-path: resources/
- name: serviceapproval
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: serviceapproval-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: serviceapproval-destination-service
    service-plan: lite
- name: serviceapproval-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: serviceapproval-xsuaa-service
    service-plan: application
- name: serviceapproval-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: serviceapproval-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
